DBSEPARATE extract a subset view of a base table from a joined view

DBPTR = DBSEPARATE ( DBPTR, TABLE )

DBSEPARATE extracts the rows from the specified base TABLE that 
participate in the view given in DBPTR. The resulting return-value
is thus a subset of the specified base table. 
