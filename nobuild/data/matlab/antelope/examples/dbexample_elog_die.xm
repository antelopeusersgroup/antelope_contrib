display('Running dbexample_elog_die')

echo on

elog_init( 'dbexample_elog_die' )

% Don't actually run this in the example, or it will do as promised
% and kill the interpreter!

% elog_die( 'This is an Antelope die message' )

echo off
