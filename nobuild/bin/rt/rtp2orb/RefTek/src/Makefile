# $Id$
# Make RefTek libraries for use with rtp2orb

#MAKE     = /opt/gnu/bin/make
MAKE     = /usr/bin/make
PLATFORM = solaris.sparc
OSNAME   = SOLARIS
OSTYPE   = SVR4
CC       = cc

all: FORCE
	cd lib; $(MAKE) PLATFORM=$(PLATFORM) OSNAME=$(OSNAME) OSTYPE=$(OSTYPE) CC=$(CC)

FORCE:
